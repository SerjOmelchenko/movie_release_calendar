{
  "name": "movie-release-calendar",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "generate": "node scripts/generate.js",
    "test": "node --test tests/*.test.js",
    "generate:cached": "REGEN_ONLY=1 node scripts/generate.js",
    "enrich": "node scripts/enrich-catalog.js",
    "providers": "node scripts/refresh-providers.js",
    "dev": "node scripts/serve.js"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
